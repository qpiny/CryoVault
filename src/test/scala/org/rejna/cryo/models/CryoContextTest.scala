package org.rejna.cryo.models

import akka.actor.ActorSystem

import com.typesafe.config.Config

class CryoContextTest(system: ActorSystem, config: Config) extends CryoContext(system, config) {

  override def 
}